"""`campus_search` lives here (tasklist B1).

Registered with @register_tool from apps.tools.registry, mode="rag". Every
result must carry `url` and `indexed_at` (PRD §4).

This module is already imported by ToolsConfig.ready(), so a decorated function
here registers itself with no further wiring.
"""
